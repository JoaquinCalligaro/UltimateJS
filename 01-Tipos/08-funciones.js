function saludar() {
  console.log('¡Hola, mundo!'); // Muestra un mensaje en la consola
}

saludar(); // Llama a la función 'saludar' para ejecutar su código

function suma() {
  return 2 + 2; // Devuelve la suma de 2 + 2
}

let resultado = suma(); // Llama a la función 'suma' y almacena el resultado en la variable 'resultado'

console.log(suma()); // Muestra el resultado de la suma en la consola
